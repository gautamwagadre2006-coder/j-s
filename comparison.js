console.log(null>0);
 // this will return false because null is not greater than 0
console.log(null<0);
    // this will return false because null is not less than 0
console.log(null==0);
   // this will return false because null is not equal to 0
console.log(null>=0);  
 // this will return true because null is considered to be equal to 0 in this case
console.log(null<=0); 
  // this will return true because null is considered to be equal to 0 in this case
console.log(null==undefined);
 // this will return true because null and undefined are considered to be equal in this case
console.log(null===undefined);
 // this will return false because null and undefined are not of the same type        