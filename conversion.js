let score="33";
console.log(typeof score);
console.log(typeof(score));

let valueInNumber=Number(score);
console.log(typeof valueInNumber);
// the Number() function is used to convert a string to a number in JavaScript.
// if the string cannot be converted to a number, it will return NaN (Not a Number).
// if the string is empty, it will return 0.
// if the string contains only whitespace, it will also return 0.
